kind: pipeline
name: default

steps:
  - name: test
    image: node
    commands:
      - npm install 
      - npm run test

  - name: e2e-test
    image: node
    commands:
      - npm install 
      - npm run test:e2e
